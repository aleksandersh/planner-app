package io.github.aleksandersh.plannerapp.app

import android.content.Context
import io.github.aleksandersh.plannerapp.utils.dip

class CachedDimens(context: Context) {

    val dip4: Int = context.dip(4)
    val dip8: Int = context.dip(8)
    val dip16: Int = context.dip(16)
}